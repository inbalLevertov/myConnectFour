<button>submit {{# if icon}} 💁🏼‍♂️ {{/if}}</button>
<!-- {{>button icon=true}}

    {{exclaim 'amazing'}} -->

    {{#if welcome}}
    {{/if}}

    <a class="title" href="/projects/{{projects.directory}}">{{projects.selectedProject.title}}</a>

    <a href="/{{directory}}"> <img src="{{directory}}/screenshot.jpg" alt="screenshot" ></a>
